const taskConfig = {
  departmentId: 'int',
  categoryId: 'int',
  status: 'string',
  raisedTo: 'string',
  closeDate: 'date',
  taskNumber: 'string',
  taskTitle: 'string'
};

module.exports = taskConfig;
